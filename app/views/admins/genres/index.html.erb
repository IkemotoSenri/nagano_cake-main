<h4>ジャンル⼀覧・追加</h4>

<%= form_with model: @genres_new, url:'/genres', local:true do |f| %>
<h5>ジャンル名</h5>
<%= f.text_field :name %>
 <%= f.submit '新規登録', class: "btn btn-primary" %>
 
<% end %>

 <%= @genres.name %>